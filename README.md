# Java-Projects
1. LIBRARY MANAGEMENT SYSTEM- Designed a library management system that uses inheritence to handle different types of users(e.g. students, professors etc.) and library items (e.g. books, magazines etc.).
   The system allow borrowing and returning of items with rules varying for different users.
